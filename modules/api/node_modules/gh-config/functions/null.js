// add modules
const fs = require('fs');

const config = require('./insideConfig');

const setToNull = () => {
    let path = config.get();
    fs.writeFileSync(path, '[]');
}

module.exports = setToNull;